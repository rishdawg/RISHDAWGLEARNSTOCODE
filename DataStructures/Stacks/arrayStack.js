const fruits = [];

fruits.push('apple'); // push
fruits.push('orange');

const length = fruits.length;

console.log(fruits[length-1]); // peek
console.log(length) // length

fruits.pop(); // pop
console.log(fruits);